<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/blue">

    <LinearLayout
        android:id="@+id/onboarding_header"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:orientation="vertical"
        android:padding="20dp"
        android:gravity="center">

        <TextView
            android:id="@+id/section_header"
            style="@style/SurveyQuestionHeader"
            android:textAllCaps="true"
            android:typeface="normal"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

        <TextView
            style="@style/SurveyQuestionSubHeader"
            android:layout_marginTop="5dp"
            android:typeface="serif"
            android:id="@+id/section_question"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

    </LinearLayout>

    <ListView
        android:id="@android:id/list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingTop="@dimen/survey_list_top_padding"
        android:paddingLeft="@dimen/survey_list_horizonal_padding"
        android:paddingRight="@dimen/survey_list_horizonal_padding"
        android:paddingBottom="@dimen/survey_list_bottom_padding"
        android:clipToPadding="false"/>
</RelativeLayout>